name: "CI — agent-portal"

on:
  push:
    paths:
      - "services/frontend/agent-portal/**"
      - "packages/**"
  pull_request:
    paths:
      - "services/frontend/agent-portal/**"
      - "packages/**"

concurrency:
  group: ci-agent-portal-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Cache npm
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-agent-portal-${{ hashFiles('services/frontend/agent-portal/package-lock.json') }}
          restore-keys: ${{ runner.os }}-node-agent-portal-
      - name: Install dependencies
        run: npm ci
        working-directory: services/frontend/agent-portal
      - name: Lint (ESLint)
        run: npm run lint
        working-directory: services/frontend/agent-portal
      - name: Format check (Prettier)
        run: npm run format:check
        working-directory: services/frontend/agent-portal

  type-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Cache npm
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-agent-portal-${{ hashFiles('services/frontend/agent-portal/package-lock.json') }}
          restore-keys: ${{ runner.os }}-node-agent-portal-
      - name: Install dependencies
        run: npm ci
        working-directory: services/frontend/agent-portal
      - name: TypeScript type check
        run: npm run type-check
        working-directory: services/frontend/agent-portal

  unit-test:
    runs-on: ubuntu-latest
    needs: [lint, type-check]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Cache npm
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-agent-portal-${{ hashFiles('services/frontend/agent-portal/package-lock.json') }}
          restore-keys: ${{ runner.os }}-node-agent-portal-
      - name: Install dependencies
        run: npm ci
        working-directory: services/frontend/agent-portal
      - name: Run unit tests with coverage
        run: npm test -- --ci --coverage --coverageThreshold='{"global":{"lines":82}}'
        working-directory: services/frontend/agent-portal
      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: agent-portal-coverage
          path: services/frontend/agent-portal/coverage/

  security-scan:
    needs: [lint]
    uses: ./.github/workflows/_template-security-scan.yml
    with:
      service_name: agent-portal
      working_directory: services/frontend/agent-portal
      severity_threshold: HIGH
    secrets: inherit

  build:
    needs: [unit-test, security-scan]
    uses: ./.github/workflows/_template-node-ci.yml
    with:
      service_name: agent-portal
      node_version: "20"
      working_directory: services/frontend/agent-portal
      run_e2e: false
    secrets: inherit

  e2e-test:
    needs: [build]
    uses: ./.github/workflows/_template-e2e-test.yml
    with:
      service_name: agent-portal
      base_url: https://staging-agent.internal
      browser_list: chromium,firefox
      test_grep_pattern: "listing|leads|showing|performance"
    secrets: inherit

  deploy-staging:
    needs: [e2e-test]
    if: github.ref == 'refs/heads/develop'
    uses: ./.github/workflows/_template-deploy-k8s.yml
    with:
      environment: staging
      service_name: agent-portal
      image_tag: ${{ github.sha }}
      cluster_name: staging-eks
      namespace: frontend
      helm_chart_path: charts/agent-portal
      replicas: 2
      wait_for_rollout: true
    secrets: inherit
