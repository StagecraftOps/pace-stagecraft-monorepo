name: CI - Listing ETL Pipeline

on:
  push:
    branches: [main, develop]
    paths:
      - "services/data/listing-etl/**"
      - ".github/workflows/ci-listing-etl.yml"
  pull_request:
    branches: [main, develop]
    paths:
      - "services/data/listing-etl/**"
      - ".github/workflows/ci-listing-etl.yml"

concurrency:
  group: "${{ github.workflow }}-${{ github.ref }}"
  cancel-in-progress: true

env:
  SERVICE_DIR: services/data/listing-etl
  PYTHON_VERSION: "3.11"
  IMAGE_NAME: listing-etl

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Cache pip dependencies
        uses: actions/cache@v6
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('services/data/listing-etl/requirements*.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install lint dependencies
        working-directory: ${{ env.SERVICE_DIR }}
        run: pip install flake8 black isort mypy

      - name: Run flake8
        working-directory: ${{ env.SERVICE_DIR }}
        run: flake8 src/ --max-line-length=120 --extend-ignore=E203

      - name: Check black formatting
        working-directory: ${{ env.SERVICE_DIR }}
        run: black --check src/

      - name: Check import ordering with isort
        working-directory: ${{ env.SERVICE_DIR }}
        run: isort --check-only src/

      - name: Run mypy type checks
        working-directory: ${{ env.SERVICE_DIR }}
        run: mypy src/ --ignore-missing-imports

  unit-test:
    name: Unit Tests
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Cache pip dependencies
        uses: actions/cache@v6
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('services/data/listing-etl/requirements*.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        working-directory: ${{ env.SERVICE_DIR }}
        run: pip install -r requirements.txt -r requirements-dev.txt

      - name: Run pytest with coverage
        working-directory: ${{ env.SERVICE_DIR }}
        run: |
          pytest tests/unit/ \
            --cov=src \
            --cov-report=xml \
            --cov-report=term-missing \
            --cov-fail-under=80 \
            -v

      - name: Upload coverage report
        uses: actions/upload-artifact@v7
        with:
          name: listing-etl-coverage
          path: ${{ env.SERVICE_DIR }}/coverage.xml

  integration-test:
    name: Integration Tests
    needs: unit-test
    uses: ./.github/workflows/_template-integration-test.yml
    with:
      service_name: listing-etl
      test_suite: integration
      test_timeout_minutes: 30
    secrets: inherit

  docker-build:
    name: Docker Build
    needs: integration-test
    uses: ./.github/workflows/_template-docker-build.yml
    with:
      service_name: listing-etl
      ecr_repository: listing-etl
      dockerfile_path: services/data/listing-etl/Dockerfile
      image_tag: ${{ github.sha }}
      push_image: true
    secrets: inherit

  deploy-staging:
    name: Deploy to Staging
    needs: docker-build
    if: github.ref == 'refs/heads/develop'
    uses: ./.github/workflows/_template-deploy-k8s.yml
    with:
      environment: staging
      service_name: listing-etl
      namespace: data-pipeline
      cluster_name: staging-cluster
      image_tag: ${{ github.sha }}
    secrets: inherit

  security-scan:
    name: Security Scan
    needs: docker-build
    uses: ./.github/workflows/_template-security-scan.yml
    with:
      service_name: listing-etl
      working_directory: services/data/listing-etl
      severity_threshold: HIGH
    secrets: inherit

  notify:
    name: Notify Slack
    needs: [deploy-staging, security-scan]
    if: always()
    uses: ./.github/workflows/_template-notify-slack.yml
    with:
      service_name: listing-etl
      environment: staging
      status: ${{ contains(needs.*.result, 'failure') && 'failure' || 'success' }}
      triggered_by: ${{ github.actor }}
    secrets: inherit

