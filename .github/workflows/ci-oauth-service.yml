name: "CI — oauth-service"

on:
  push:
    paths:
      - 'services/auth/oauth-service/**'
      - 'packages/**'
  pull_request:
    paths:
      - 'services/auth/oauth-service/**'
      - 'packages/**'

concurrency:
  group: ci-oauth-service-${{ github.ref }}
  cancel-in-progress: true

env:
  SERVICE_DIR: services/auth/oauth-service
  NODE_VERSION: '20'

jobs:
  lint:
    name: Lint & Format Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Cache npm dependencies
        uses: actions/cache@v6
        with:
          path: ~/.npm
          key: oauth-${{ hashFiles('services/auth/oauth-service/package-lock.json') }}
          restore-keys: oauth-

      - name: Install dependencies
        working-directory: ${{ env.SERVICE_DIR }}
        run: npm ci

      - name: Run ESLint
        working-directory: ${{ env.SERVICE_DIR }}
        run: npm run lint

      - name: Check formatting
        working-directory: ${{ env.SERVICE_DIR }}
        run: npm run format:check

  unit-test:
    name: Unit Tests
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Cache npm dependencies
        uses: actions/cache@v6
        with:
          path: ~/.npm
          key: oauth-${{ hashFiles('services/auth/oauth-service/package-lock.json') }}
          restore-keys: oauth-

      - name: Install dependencies
        working-directory: ${{ env.SERVICE_DIR }}
        run: npm ci

      - name: Run unit tests with coverage
        working-directory: ${{ env.SERVICE_DIR }}
        run: |
          npm test -- \
            --coverage \
            --coverageThreshold='{"global":{"lines":85}}' \
            --forceExit \
            --ci

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v7
        with:
          name: oauth-coverage
          path: ${{ env.SERVICE_DIR }}/coverage/
          retention-days: 7

  security-scan:
    name: Security Scan
    needs: lint
    uses: ./.github/workflows/_template-security-scan.yml
    with:
      service_name: oauth-service
      working_directory: services/auth/oauth-service
      severity_threshold: HIGH
    secrets: inherit

  build:
    name: Build
    needs: [unit-test, security-scan]
    uses: ./.github/workflows/_template-node-ci.yml
    with:
      service_name: oauth-service
      node_version: '20'
      working_directory: services/auth/oauth-service
      run_e2e: false
    secrets: inherit

  docker-build:
    name: Build Docker Image
    needs: build
    uses: ./.github/workflows/_template-docker-build.yml
    with:
      service_name: oauth-service
      dockerfile_path: services/auth/oauth-service/Dockerfile
      ecr_repository: stagecraft/oauth-service
      image_tag: ${{ github.sha }}
      push_image: ${{ github.ref == 'refs/heads/develop' || github.ref == 'refs/heads/main' }}
    secrets: inherit

  integration-test:
    name: Integration Tests — OAuth Flows
    needs: docker-build
    uses: ./.github/workflows/_template-integration-test.yml
    with:
      service_name: oauth-service
      test_suite: oauth-flows
      environment_url: http://localhost:8080
      test_timeout_minutes: 20
    secrets: inherit

  deploy-staging:
    name: Deploy to Staging
    needs: integration-test
    if: github.ref == 'refs/heads/develop'
    uses: ./.github/workflows/_template-deploy-k8s.yml
    with:
      environment: staging
      service_name: oauth-service
      image_tag: ${{ github.sha }}
      cluster_name: staging-eks
      namespace: auth
      helm_chart_path: charts/oauth-service
      replicas: 2
      wait_for_rollout: true
    secrets: inherit
